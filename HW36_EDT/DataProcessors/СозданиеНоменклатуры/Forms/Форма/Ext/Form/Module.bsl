
&НаСервереБезКонтекста
Процедура СоздатьНаСервере(Группа)
	
	Для Счетчик = 1 По 10000 Цикл
	
		Элемент = Справочники.Номенклатура.СоздатьЭлемент();
		Элемент.ВидНоменклатуры = Перечисления.ВидыНоменклатуры.Товар;
		Элемент.Родитель = Группа;
		Элемент.Наименование = "Товар " + Формат(Счетчик, "ЧЦ=5; ЧВН=; ЧГ=0");
		Элемент.Записать();
	
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Создать(Команда)
	СоздатьНаСервере(Объект.Группа);
КонецПроцедуры
